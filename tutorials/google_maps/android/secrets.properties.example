# Copy to secrets.properties (gitignored) and add your own key.
# Android Google Maps key — read by app/build.gradle.kts.
# Lives here (NOT local.properties) because the dn tool rewrites
# local.properties on every run and drops unknown keys.
MAPS_API_KEY=YOUR_ANDROID_MAPS_API_KEY
