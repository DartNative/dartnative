import UIKit
import dartnative_ios

@main
@objc class AppDelegate: DartNativeAppDelegate {
  // The base class owns the engine lifecycle; the window is built in
  // SceneDelegate. Override didCreateEngine(_:) for custom native setup.
}
