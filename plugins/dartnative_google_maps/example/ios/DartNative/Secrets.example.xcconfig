// Copy this file to Secrets.xcconfig (gitignored) and set your iOS Google Maps key.
// It flows: Secrets.xcconfig → MAPS_API_KEY build setting → Info.plist (GMSApiKey)
// → AppDelegate.swift GMSServices.provideAPIKey(). No key is committed.
MAPS_API_KEY = your-ios-google-maps-api-key
