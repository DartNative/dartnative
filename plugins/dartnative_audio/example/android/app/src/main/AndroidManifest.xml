<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- INTERNET is auto-merged from the plugin manifest (ExoPlayer-safe). -->

    <!-- RECORD_AUDIO is auto-merged from the plugin manifest, but list it
         again here so it's visible in this example's manifest. The runtime
         permission still has to be requested by the host before
         AudioRecorder.start() — see lib/screens/audio_demo.dart. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:label="Audio"
        android:name=".Application"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustNothing">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>
</manifest>
