<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- API 29+: disable the system's automatic contrast-enforcement scrim
         that makes transparent bars appear dark-grey instead of showing the
         actual content behind them. Must be set at theme level — runtime-only
         isNavigationBarContrastEnforced=false is ignored on some OEM skins. -->
    <style name="NormalTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
    </style>
</resources>
